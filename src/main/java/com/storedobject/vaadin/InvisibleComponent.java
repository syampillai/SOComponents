package com.storedobject.vaadin;

/**
 * A marker interface to denote that a {@link com.vaadin.flow.component.Component} is invisible (doesn't have width and height).
 *
 * @author Syam
 */
public interface InvisibleComponent {
}
